import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-skill-matrix',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './skill-matrix.html',
  styleUrls: ['./skill-matrix.css']
})
export class SkillMatrix {}
